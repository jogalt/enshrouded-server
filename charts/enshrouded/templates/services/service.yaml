---
apiVersion: v1
kind: Service
metadata:
  name: enshrouded
  namespace: enshrouded
  annotations:
    {{- if eq .Values.service.type "cilium" }}
    {{- range $key, $value := .Values.cilium.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- else if eq .Values.service.type "nginx" }}
    {{- range $key, $value := .Values.nginx.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
    {{- end }}
spec:
  type: {{ if eq .Values.service.type "cilium" }}{{ .Values.cilium.type }}{{ else }}{{ .Values.nginx.type }}{{ end }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      protocol: {{ .protocol }}
      name: {{ .name }}
      targetPort: {{ .targetPort }}
    {{- end }}
  selector:
    app: enshrouded
